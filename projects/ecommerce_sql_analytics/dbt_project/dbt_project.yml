name: 'ecommerce_analytics_engineering'
version: '1.0.0'
config-version: 2

profile: 'ecommerce_analytics_engineering'

model-paths: ['models']
test-paths: ['tests']
target-path: 'target'
clean-targets: ['target', 'dbt_packages']

models:
  ecommerce_analytics_engineering:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
